[project]
name = "iz_chat"
description = "LLM chat node for ComfyUI — chat with Vision-Language models directly in the node UI, no workflow execution needed for generation."
version = "1.0.0.0"
license = {file = "LICENSE"}
dependencies = []

[project.urls]
Repository = "https://github.com/IZmake/iz_chat"

[tool.comfy]
PublisherId = "izmake" 
DisplayName = "IZ chat"
Icon = "https://raw.githubusercontent.com/IZmake/iz_chat/refs/heads/main/logo.png"
